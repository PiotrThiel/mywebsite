Aplikacje Gita:
- GitK (Git repository broswer) - pokazuje historiê repo, czyli to co siê dzia³o: ga³êzie, diff, commity. Wizualna alternatywa dla >git log
- Git GUI (Git graphical inteface) - pokazuje aktualny stan naszego repozytorium: aktualne zmiany, mo¿liwoœæ zapisu zmian)
-SHIFT + PKM na katalogu repo - rozszerzone menu kontekstowe np Open command window here


Definicje:
• commit - stan repozytorium w danym momencie. Konkretna WERSJA projektu w danym momencie czasu:
	- oznaczony wyliczonym identyfikatorem SHA1
	- ca³a historia projektu sk³ada siê z commitów
	- oprócz plikó zawiera tak¿e informacje o autorze oraz "commit message" (opisu zmian)
	- pierwszu commit, punkt pocz¹rkowy repo nazywa siê root.
	- root mo¿e zawieraæ tylko opis projektu w pliku readme.md (jêzyk **markdown**). Jêzyk w którym tekst formatujêsiê za pomoc¹ giazdek (*): 
	  **pogrubienie**, *pochylenie*, _podkreœ³enie_
• branch (ga³¹Ÿ) - Kopia repozytorium, umo¿liwiaj¹ca pracê niezale¿n¹ od pozosta³ych kopii.
	- ruchomy wskaŸnik na commit (zmienia siê z ka¿dym kolejnym commitem)
	- kopia "koncepcyjna", a nie fizyczna (Git nie duplikuje plików miêdzy ga³êziami)
	- ga³¹Ÿ - to lista commitów. Z czego jeden jest szczególny to TIP.

• tip	- tip - Ostatni (najnowszy) commit w ga³êzi.
	Charakterystyki ga³êzi w Gicie
		- w commicie nie ma zapisanej informacji na jakiej ga³êzi zosta³ utworzony
		- commity mo¿na przenosiæ miêdzy ga³êziami
		- commit mo¿e zawieraæ siê w wielu - lub ¿adnej - ga³êzi
		- ga³êzie nie maj¹ statusu "aktywne / zamkniête"
		- ga³êzie siê po prostu tworzy i kasuje
• head	- (ma³e litery) jest to wskaŸnik na tip (commit)
	- nazwana referencja; nazwa jest jednoczeœnie nazw¹ ga³êzi,
	- mamy tyle hedów ile mamy zdefiniowanych ga³êzi w repo
• HEAD	- (du¿e litery) jest to commit bêd¹cy Ÿród³em aktualnej zawartoœci Working Copy (aktualny commit).
	- aktualny commit, czyli ostotnio wyczekoutowany commit (git checkout)
	- skrót (alias) do HEAD jest '@' (ma³pka)
	-jaki commit jest Ÿród³em plików znajduj¹cych siê w katalogu 'working directory'
• master - domyœlna nazwa pierwszej ga³êzi w repozytorium Gita. (w innych VCS: trunk, Main, default)
• merge	- po³¹czenie dwóch (lub wiêcej) ga³êzi poprzez przeniesienie zmian z jednej do drugiej.
  • Fast-forward merge = Commity ze Ÿród³owej ga³êzi s¹ przeniesione bez zmian i dodatkowych operacji na docelow¹ ga³¹Ÿ. 
	- FF merge nie zostawia œladu w historii.
	  Kiedy FF merge jest mo¿liwy?
	- FF merge jest mo¿liwy tylko wtedy, gdy ga³¹Ÿ DOCELOWA (ta która przyjmie zmiany) nie posiada commitów spoza ga³êzi RÓD£OWEJ (ta od której bêdzie siê bra³o zmiany).
	- albo inaczej: gdy na ga³êzi docelowej nie wykonano git commit od czasu utworzenia ga³êzi Ÿród³owej.
	- commity "base" == "target"
  • 3-way merge - jak dzia³a? Commit po commicie. Plik po pliku. Kawa³ek po kawa³ku. Dla uproszczenia: linia po linii. 
	  Co znaczy "3-way"?
	  3 "ways":
	- "base" - najpóŸniejszy wspólny commit dla obu ga³êzi
	- "source" - wersja do z³¹czenia (ga³¹Ÿ Ÿród³owa)
	- "target" - wynikowy plik (mo¿e zawieraæ w³asne zmiany)
	  Algorytm:
	- jeœli linia z "base" jest taka sama w "source" i "target" zostawiamy jak jest	
	- jeœli linia z "base" jest zmieniona (lub usuniêta) w jednym z "source" albo "target" przenosimy zmiany
	- jeœli linia z "base" jest zmieniona (lub usuniêta) w obu "source" i "target" KONFLKT!
  • Octopus merge - po³¹czenie wiêcej ni¿ 2 ga³êzi. Powinno siêich unikaæ.
• parent - poprzedni commit (rodzic)
	- Commit mo¿e mieæ wielu rodziców, jeœli jest efektem operacji merge.
	- root commit - pierwszy commit w historii (root commit) nie ma  
• repozytorium - to katalog na dysku zawieraj¹cy wszystkie informacje o aktualnym stanie projektu i jego historii. Mo¿e zawieraæ odwo³ania do innych repo.
• tag - nieruchomy wskaŸnik, referenja na konkretny commit. Jest to etykieta, nazwa, któr¹ mo¿emy nadaæ commitowi bez zmiany jego historii
	- raz nadany zawsze wskazuje na to samo miejsce w historii, chyba ¿e zostanie skasowany i utworzony od nowa.
	- podzia³ tagów:
		- lightweight (lekkie) 
			- doczepione s³owo do commita. 
			- rekomendowane jako tymczasowe (prywatne) zapamiêtanie commita, które i tak zaraz bêd¹ kasowane
			- zastosowanie:
				- przygotowuj¹c skomplikowan¹ operacjê czasami wygodnie jest tymczasowo NAZWAÆ commity (tag = label)
				- automatycznie nadany numer wersji przed wdro¿eniem przez build server
		- annotaded (ciê¿kie, onotowane)
			- zawiera wszystko, co lightweight
			- plus informacje takie jak posiada commit:
				- autor taga
				- "tag message"
				- data utworzenia
				- zastosowanie:
				- publiczne komentarze do commitów bez modyfikacji historii
				- (prawie) zawsze lepiej u¿yæ tego ni¿ lightweight tag

• Workin copy lub 'Working director' (pl. katalog roboczy) - katalog z plikami na których aktualnie siê pracuje.
	- w nim umieszczana jest zawartoœæ wybranego repozytorium, czyli tego na którym aktualnie pracujemy
	- ze zmian w 'Working copy' tworzone s¹ kolejne wersje repozytorium czyli kolejne commity





COMMANDS------------------------------------------------------------------------------------------------------------------------ ---------------------------------------------------

>git add 
	readme.md					- komend¹ 'add' wykonujemy do wszystkich nowych plików dodawanych do projektu (repo). W tym przypadku pliku "readme.md"
	.						-'.' dodanie wszystkich nowych plików do repo
>git branch 						- lista ga³êzi, gdzie * oznacza aktualna ga³¹Ÿ w Working Copy
	my_new_branch 					- tworzy now¹ ga³¹Ÿ
	* 						- (gwiazdka) aktualna ga³¹Ÿ w Working Copy
	-d <branch> 					- kasuje ga³¹Ÿ
	-m <new_name_branch)				- zmiana nazwy ga³êzi w aktualnym WD

>git checkout						- wskazuje aktualny commit w Working Directory
	<branch> 					- za³adowanie do Workong Copy danej ga³¹zi
	<tag> 						- za³adowanie do Workong Copy commita po zadanym tagu
	<SHA> 						- za³adowanie do Workong Copy commita po zadanym HASH (ID)
	<ref> 						- za³adowanie do Workong Copy... commita podany w dowolny sposób (np. git checkout master~4)
	-						- umieszcza poprzedni commit w Working Directory (czyli poprzedni HEAD)
	-- 						- umieszcza do Working Copy stan pliku z innego commita (bez zmiany ga³êzi)
							  Innymi s³owy jeœli do WC damy plik z commita poprzediego i zrobimy nowego commita - 
							  to zostanie przywrócaony stan pliku z tego poprzedniego commiata.
							  Po co -- ?  bo git checkout index.html te¿ zadzia³a
							  Na wypadek gdy plik nazywa siê tak samo jak ga³¹Ÿ: git checkout -- master  (jeœli mamy plik o nazwie "master")
	<commit> <path>					- jw
	-b <new_branch>					- za³o¿enie nowej ga³êzi i od razu przejœcie do niej
	@^						- jest to chechout commita, który nie jest tipem brancha czyli stan Detached HEAD
	ll						- j.w.  Detached HEAD - oznacza ¿e nie jesteœmy na ¿adnej ga³êzi wiêc commity tak¿e nie bêd¹ na ¿adnej ga³êzi.
								¯adna etykieta, referencja ga³êzi nie wskazuje na aktualny commit wiêc nie bêdzie siê posuwa³a w miarêbudowania historii
								Detached HEAD - stan, w którym zawartoœæ Working Copy (HEAD) to commit niebêd¹cy tipem (ostatnim commitem w ga³êzi).
								Powrót z tego stanu to $git checkout -


>git clone ADRES .					- kropka mówi, ¿e ma byæ projekt sklonowany do tego katalogu w którym odpalona jest konsola
							- clone wykonujemy tylko JEDEN raz
>git commit 	(lub >git help commit)	
	--help 						-otworzy siê strona z dokumentacj¹ do commita
	.						-'.' 
	. -m "Moja zmiana"				- m "komentarz" - (message)
							- zmiany s¹ zapisane w moim repo, na moim dysku
							- komenda commit nie dodaje nowych plików
							- Mo¿emy przegl¹daæ kod, ale nie ma sensu commitowaæ - nie jesteœmy na ¿adnej ga³êzi... 
							  


>git config
	--global user.name "Iniê Nazwisko"		- nazwa u¿ytkownika ale globalnie
	--global user.email thiel.piotr@gmail.com 	- mail u¿ytkownika, ale globalnie
	--global diff.tool winmerge			- przypisane do gita narzêdzie do porównywania plików: winmerge
	--global difftool.prompt false			- wy³¹czenie powtarzaj¹cych sie informacji w narzêdziu difftool
	core.editor notepad				- ustawienie domyœlnego edytora gita
>git diff						- pokazuje zmiany w plikach w konsoli (przed wykonaniem commit'a)
>git difftool						- pokazuje zmiany w pikach w wizualnym narzêdziu jaki zosta³ do gita przypisany
>git init						- git inicjalizyje repo i tworzy ukryty katalog .git
>git log 						- pokazuje historiê projektu (commity)... w konsoli
	-p						- pokazuje ona ró¿nice wprowadzone z ka¿d¹ rewizj¹. 
	--stat						- Pokazuje pliki, które zosta³y zmienione wraz z liczb¹ dodanych i usuniêtych linii w kodzie. 
							  Podaje równie¿ podsumowanie z informacj¹ ile plików zosta³o zmienionych, 
	-n4						- pokazuje ostatnie 4 commity
	--oneline					- pokazuje commity w jednej linii (tylko komentarze commitów)
	--since='{15 minutes ago}'			- pokazuje comity z ostatnich 15 minut
	--since='{1 hour ago}'				- pokazuje comity z ostatiej godziny
	--author="Piotr Thiel"				- filtr pokazuj¹cy co robi³a konkretna osoba
	--grep="tests"					- filtr do przeszukiwania informacji wpisanych przy commicie
							  ile linii we wszystkich plikach zosta³o dodanych b¹dŸ usuniêtych.
	--graph    					- poka¿ graf ASCII ga³êzi oraz historiê scaleñ obok wyniku.
	-- git-rebase.sh				- historia wybranego pliku
	--all --oneline					- wszystkie commity niezale¿nie od ga³êi w jakiej jesteœ

..show	tag1..tag2 					- '..' od -do. Wszystkie commity "od" tag1 "do" tag2" (commity bêd¹ce przed tag2, niebêd¹ce przed tag1). 
							  Od póŸniejszych do wczeœniejszych. GitK od do³u do góry, bez dolnego.
 	my_tag^^^..my_tag				- komity od my_tag-3 poziony do my_tag
	foo bar ^baz					- oznacza "wypisz wszystkie commity, które s¹ osi¹galne z foo lub bar, ale nie z baz", gdzie ^ - jest wy³¹czeniem ze zbioru
>gitk 							- graficzny interfejs, pokazuje ga³¹Ÿ na której domyœlnie siê znadujemy
	--all						- pokazuje szystkie ga³êzie
>git merge						- po³¹czenie dwóch (lub wiêcej) ga³êzi poprzez przeniesienie zmian z jednej do drugiej.
>git pull						- pobranie zmian innych cz³onków zespo³u
>git push						- wypchniêcie/wys³anie zmian do repo z którego zosta³ pobrany projekt
							  Po wypchniêciu zmainy s¹ widoczne w repo z którego zosta³ pobrany projekt
>git status						- sprawdza status katalogu 'Working copy': nowe pliki, zmodyfikowane, usuniête. 
							  Jeœli git status jest d³u¿szy ni¿ 1 ekran to najwy¿szy czas zrobiæ commit
>git show						- bez ¿adnych parametrów pokazuje informacje o aktualnym commicie w WD (working directory)
	HEAD						- jw
	@						- jw alias do HEAD
	<tag>						- pokazuje informacje o podanym tagu
	<commit>					- pokazuje zawartoœ commita i jego tagów
	<commit> ~ lub ^ 				- referencje do poprzednich commitów, gdzie ^ ang. karet, przyk³ady:
		- <commit>~X 				- X commitów wczeœniej
		- <commit>^^^^ 				- 4 commity wczeœniej
		-p: git show master~2			- 2 commity wczeœniej
	@@{'5 minutes ago'}				- w jakim commicie "by³em" 5 minut temu? 3 tygodnie temu? 13 dni temu? Druga ma³pka informuje, ¿e bêdzie podny parametr czasowy
	HEAD@{1} = git show @@{1}			- jaki by³ przedostatni "wycheckoutowany" commit?

>git tag 						- wypisanie wszytkich tagów
	finished_ini					- oznaczenie commita, który jest koñcem naszej inicjalizacji projektu
	<nazwa> 549ba15					- otagowanie commita z haszem=549ba15
	<nazwa>						- nadaje tag o nazwie <nazwa> aktualnemu commitowi
	<nazwa> <commit>				- nadaje tag lekki o nazwie <nazwa> commitowi o haszu <commit>
	<nazwa> <commit> -a -m "msg"			- nadaje tag ciê¿ki o nazwie <nazwa> commitowi o haszu <commit> i wiadomoœci ('-m') "msg"
	-d <nazwa>					- kasuje tag o podanej nazwie
>git show
>git version 						- sprawdzenie zaistalowanej wersji gita: CMD
>ls							- lista plików
>notepad readme.md 					- utworzenie pliku .md 
>touch new_file.txt					- komenda 'touch' tworzy nay plik




KONFIGURACJA gita:-------------------------------------------------------------------------------------
>git config --global user.name "Iniê Nazwisko"		- nazwa u¿ytkownika. Cudzys³ów u¿ywamy jak jest spacja miêdzy wyrazami
		- global 				- bez tego konfiguracja repo w którym aktualnie siê znajdujemy
		- global 				- z tym, to jest robiona konfiguracja wszystkich moich repo (repozytorium)
>git config --global user.email thiel.piotr@gmail.com	
		- mail u¿ytkownika
>git convig core.ditor notepad 				- ustawienie domyœlnego edytora gita nan notatnik (VIM jest domyœ³ny)
>git config --global diff.tool winmerge 		- dodanie winmerge jako narzêdia do porównywania plików. Nale¿y go wczeœniej zainstalowaæ w Windowsie.

Weryfikacja gita:
>git config user.name					- nazwa u¿ytkownika gita
>git config user.email					- email u¿ytkownika gita
>git convig core.ditor					- domyœlny edytor gita
>git config.tool <tool name?				- konfiguracja narzêdzia 
>git config diff.tool 					- zwraca przypisane do gita narzêdzie do porównywania plików. Tutaj: winmerge



INICJALIZACJA (za³o¿enie)REPOZYTORIUM:------------------------------------------------------------------
$mkdir my_projects					- utworzenie katalogu
$cd my_projekts						- przejœcie do tego katalogu
$git init						- inicjalizacja katalogu jako repozytorim ('init' tworzy ukryty katalog .git)
$git convig core.ditor notepad				- ustawienie domyœlnego edytora gita

							Tworzenie projektu:
$notepad readme.md 					- utworzenie pliku .md 
$git add readme.md					- dodanie/zapisanie pliku w repozytorium
$git commit 						- dodanie opisu zmian (commit message) 
$touch new_file.txt					- daodanie nowego pliku .txt (new_file)		- modyfikacja stanu Workin copy
$notepad readme.md					- otworzenie pliku i dokonanie jakiœ zmian	- modyfikacja stanu Workin copy
$git status						- sprawdza statis 'Working copy': nowe pliki, zmodyfikowane, usuniête
$git commit .						- commit dla wszystkich '.' zmian
$git diff						- pokazuje wszystkie lokalne zmiany w plikach w konsoli
$git config --global diff.tool winmerge			- przypisane do gita narzêdzie do porównywania plików: winmerge
$git difftool						- uruchomienie narzedzia
$git config --global diftool.prompt false 		-wy³¹czenie powtarzaj¹cych sie informacji w narzêdziu difftool (winmerge)
$git log 						- pokazuje historiê projektu (commity)... w konsoli
$q							-'q' (quick) - wyjœcie z widoku logu
$git log --oneline					- wszystkie commity po jednej linii (tylko komentarze commita)
$gitk							- wiualna wersja zmian w repozytorimu (commity). rzeba zamkn¹æ program, aby wpisywaæ nowe komendy
$git tag finished_ini					- oznaczenie commita, który jest koñcem naszej inicjalizacji projektu
$git log --oneline
$gitk
$ git log --oneline
	d75954d (HEAD -> master, tag: finished_ini) Add year intoo footer
	e53de1f delete year from copyright in footer
	b8e9dcb removed sign in box from my page
	549ba15 Change Hello World welcome text
	684cde9 Modified website title
	4e6b8b6 Addet initial project template
	4882ece Add new file new_file.txt
	b648627 Add new file plik1.txt and make cznages in files: readme.md
	d42a077 Initial commit with project description file

$git tag no_hello 549ba15				- otagowanie commita o hsah=549ba15. Komit lekki

$ git log --oneline
	d75954d (HEAD -> master, tag: inished_ini) Add year intoo footer
	e53de1f delete year from copyright in footer
	b8e9dcb removed sign in box from my page
	549ba15 (tag: no_hello) Change Hello World welcome text
	684cde9 Modified website title
	4e6b8b6 Addet initial project template


$ git tag						- wypisanie wszystkich tagów
	finished_ini
	no_hello

$ git tag title_mod 684cde9 -a -m "previous title was from template"		- '-a' annotaded: tag ciê¿eki, 'title_mod' - nazwa taga
$ git tag
	finished_ini
	no_hello
	title_mode
$ git log --oneline
	d75954d (HEAD -> master, tag: finished_ini) Add year intoo footer
	e53de1f delete year from copyright in footer
	b8e9dcb removed sign in box from my page
	549ba15 (tag: no_hello) Change Hello World welcome text
	684cde9 (tag: title_mode) Modified website title
	4e6b8b6 Addet initial project template

$ git tag --oneline no_hello				- wypisanie logu od konkretnego taga. Tutaj od no_hello
	549ba15 (tag: no_hello) Change Hello World welcome text
	684cde9 (tag: title_mode) Modified website title
	4e6b8b6 Addet initial project template
$ git show no_hello					- informacje o commicie no_hello

$ git tag -d no_hello					- skasowanie tagu no_hello
	Deleted tag 'no_hello' (was 549ba15)
$ git tag -d title_mode					- skasowanie tagu title_mode
	Deleted tag 'title_mode' (was 726f96a)

$ git show master~2					- przejœcie o 2 comity przed commit master
$ git show master^^					- przejœcie o 2 comity przed commit master
$ git log 

$ git log --oneline o..h				-pokazanie logów od commita od taga o do g
	e53de1f (tag: h) delete year from copyright in footer
	b8e9dcb (tag: e) removed sign in box from my page
	549ba15 (tag: ll) Change Hello World welcome text
$ git log --oneline o^..h				-pokazanie logów od commita od taga o-1 poziom do g
	e53de1f (tag: h) delete year from copyright in footer
	b8e9dcb (tag: e) removed sign in box from my page
	549ba15 (tag: ll) Change Hello World welcome text
	684cde9 (tag: o) Modified website title

$ git branch my_new_branch				- utworzenie nowej ga³êzi o nazwie: my_new_branch
$ git branch						- lista ga³êzi
	* master					- * oznacza aktualna ga³¹Ÿ w Working Copy
  	branch1
	branch2

$ git branch
	  branch1
	* branch2
	  master
	  zzz

$ git branch -d branch1					- skaswanie ga³êzi branch1
	Deleted branch branch1 (was a2b70e1).

$git show 						- HEAD wskazuje commit bêd¹cy Ÿród³em aktualnej zawartoœci working cop
$git show HEAD						- HEAD j.w.
$git show @						- HEAD j.w.

$ git checkout @^					- Detached HEAD - oznacza ¿e nie jesteœmy na ¿adnej ga³êzi 

$ git checkout @^ --  docs/examples/jumbotron/index.html -przywraca zmiany z ostatniego commita na pliku index.html

$ git checkout  zzz					- za³adowanie do WD ga³êzi zzz
	Switched to branch 'zzz'

$ git branch -m aaa					- zmiana nazwy za³¹dowanej ga³êzi z 'zzz' na 'aaa'
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (aaa)

$ git branch						- wylistowanie ga³êzi
	* aaa
  	 branch2
  	 branch3
 	 master

$ git log --oneline					-- listing commitów
	3b5e763 (HEAD -> master) removd data from footer again
	a2b70e1 (tag: xyz, branch2, aaa) xyz
	d75954d (tag: finished_ini, branch3) Add year intoo footer
	e53de1f (tag: h) delete year from copyright in footer
	b8e9dcb (tag: e) removed sign in box from my page
	549ba15 (tag: ll) Change Hello World welcome text
	684cde9 (tag: o) Modified website title

$ git checkout o -- docs/examples/jumbotron/index.html	- za³adowanie pliku index.html z commita o tagu 'o' do WD

$ git status						- sprawdzenie ED
On branch master
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        modified:   docs/examples/jumbotron/index.html


$ git commit -m "recover websute title"			- zrobienie nowego commita z pliku ze starego (Przywrócenie zmian w pliku ze wskazanego commita)  
[master 67e615f] recover websute title
 1 file changed, 13 insertions(+), 3 deletions(-)

Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (master)
$ git log --0neline
fatal: unrecognized argument: --0neline

Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (master)
$ git log --oneline
67e615f (HEAD -> master) recover websute title
3b5e763 removd data from footer again
a2b70e1 (tag: xyz, branch2, aaa) xyz
d75954d (tag: finished_ini, branch3) Add year intoo footer
e53de1f (tag: h) delete year from copyright in footer
b8e9dcb (tag: e) removed sign in box from my page
549ba15 (tag: ll) Change Hello World welcome text
684cde9 (tag: o) Modified website title

**Lekcja 4.09 Merge ***************************************************************************************************************************************************
$ git checkout -b login_colors				- nowa ga³êŸ + zmiany w indeksie (YELLOW)
$ git commit . -m "changed signin button to yelow"	- nowy commit na tej ga³êzi
$ git checkout -b login_colors2				- nowa ga³êŸ + zmiany w indeksie (GREY)
$ git commit . -m "changed signin button to grey"	- nowy commit na tej ga³êzi
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors2)
$gitk							- pokazuje projekt z lokalnej ga³êzi
$ gitk --all						- pokazuje projekt z ujêciem wszystkich ga³ê¿i

$ git log -n4 --all --oneline				- ostatnie 4 commity z uwzglêdnieniem wszystkich ga³êzi
	0d70109 (login_colors2) changed signin button to grey
	c2f4671 (HEAD -> login_colors) changed signin button to yelow
	0a3c10a (master) Lekcja 4.08 Ga³êzie + dodanie pliku Git - kurs.txt
	25f3e66 recover tag ll
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors)

$ git checkout login_colors2				- przejœcie do ga³êzi DOCELOWEJ z kolorem Szarym przycisku, 
	Switched to branch 'login_colors2'		  czyli przechodzimy, na ga³¹Ÿ do której bêd¹ przypisywane zmiany z innej ga³êzi poprzez MERGE

$ git merge login_colors				- na ga³¹Ÿ login_colors2 za³adowana aktualnie do WC przenosimy zmiany z ga³êzi RÓD£OWEJ login_colors
	Already up to date.
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors2)
							- !!! NIE ZROBI£O MERGE - bo ga³¹Ÿ 'login_colors2' jest 

$ git checkout login_colors				- przejœcie do ga³êzi DOCELOWEJ
	Already on 'login_colors'
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors)

$ git merge login_colors2				- £¹czenie z ga³êzi RÓD£OWEJ (czyli z wersji do z³¹czenia)
 							  z³¹czono ga³êzie login_colors i login_colors2 poprzez przypisanie do login_colors zmian w login_colors2
	Updating c2f4671..0d70109			  GREY -> Yellow (wyœwietla Grey)
	Fast-forward
	docs/examples/jumbotron/index.html | 2 +-
	1 file changed, 1 insertion(+), 1 deletion(-)

$ git log -n4 --all --oneline				- po z³¹czeniu nie ma ju¿ osobnych commitów
	0d70109 (HEAD -> login_colors, login_colors2) changed signin button to grey
	c2f4671 changed signin button to yelow
	0a3c10a (master) Lekcja 4.08 Ga³êzie + dodanie pliku Git - kurs.txt
	25f3e66 recover tag ll
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors)

$ git branch -d login_colors2				- kasuje niepotrzeb¹ eksperymentaln¹ ga³¹Ÿ
	Deleted branch login_colors2 (was 0d70109).
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors)
	Piotr@laptopAsus MINGW64 /c/DEV/Kurs_gita/my_projects (login_colors)

$ git log -n4 --all --oneline				- ostatnie 4 commity. Widaæ, ¿e projekt wygl¹da tak, jaby nie by³a tworzona nowa ga³¹Ÿ i commit
	0d70109 (HEAD -> login_colors) changed signin button to grey
	c2f4671 changed signin button to yelow
	0a3c10a (master) Lekcja 4.08 Ga³êzie + dodanie pliku Git - kurs.txt
	25f3e66 recover tag ll


test


DeepL:================================================================================================
Ctrl + cc - t³umaczenie tekstu w t³umaczu DeepL
F9 - t³umaczenie tekstu w dowolnej aplikacji
F8 - T³umaczenei tekstu ze zrzutu ekranu
TRIKI==================================================================================================
1. WIN+3 - szybkie uruchomienie konsoli	 
	- przypi¹æ skrót do konsoli do paska zadañ jako trzeci¹ ikonê. Wtedy wywoa³nie konsili to kombinacja klawiszy WIN + nr ikony na pasku zadañ
	- ustawienia skrótu - Element docelowy: "C:\Program Files\Git\git-bash.exe"
	- ustawienia skrótu - Rozpocznij w: c:\DEV	(katalog repo)
2. Autokomplete poprzez Text Expanding - automatyczne uzupe³nianie tekstu
	- program zewnêtrzny AutoHotKey na stronie autohotkey.com
	- przypisane skrótów do do programu AutoHotKey - 2 x klikn¹c na zmodyfikowanym pliku .ahk
	- Przypisane aktualnie skróty:
; **git-specific
::ga::git add -p
::gai::git add -i
::gb::git branch
::gc::git commit
::gch::git checkout
::gcae::git commit --allow-empty
::gch::git checkout
::gd::git diff
::gdt::git difftool
::gf::git fetch
::gignore::git update-index --assume-unchanged
::gignore::git update-index --assume-unchanged
::gl::git ll
::gmnff::git merge --no-ff -e -m "" -
::gmt::git mergetool
::gr::git rebase -i 
::gs::git status
::gst::git stash
::gsh::git show
::gunignore::git update-index --no-assume-unchanged
::-h::--help

3. | clip	- Kopiowanie do schowka informacji z gita (g³ównie identyfikatorów commitów w gicie)
>git config user.name | clip	- to zostanie zwrócona bezpoœrednio do schowka zwracana informacji z gita (| - ang. pipe: rura)
	- w tym przypadku to: Piotr Thiel

